

#text attribut : It returns inner text of the element if it is present otherwise it will not print any value
#get_attribut(): IT retrurns values of any attribute of web element, for this we need to pass the
# attribute inside the brackets, and both use web element not from the driver object.
